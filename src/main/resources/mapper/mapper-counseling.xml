<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.site.mapper.CounselingMapper">

	<!-- insert -->
	<insert id="insertCounselingRequest">
       <![CDATA[
       insert into counseling_request values(
		#{sido},#{cusHuman},#{cusPrice},#{name},#{sex},
		#{phone1},#{phone2},#{phone3},#{cusEnter},#{contact_point},
		#{content}, sysdate)
	   ]]>
	</insert>
</mapper>

